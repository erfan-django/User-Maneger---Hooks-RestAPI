import UserManager from "../component/user-maneger";

export default function HomePage() {
    return (
        <main className="min-h-screen flex items-center justify-center">
            <UserManager />
        </main>
    );
}
